{
  "dependencies": {
    "fs-extra": "^11.1.1",
    "puppeteer": "^20.2.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/node": "^20.2.1",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "build:pptr": "ts-node pptr.ts",
    "build:rpl": "ts-node replace-urls.ts",
    "build:cpy": "ts-node copy-file.ts",
    "build": "npm run build:pptr && npm run build:rpl && npm run build:cpy"
  }
}